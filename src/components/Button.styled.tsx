import styled from "styled-components";

const Button = styled.button`
  padding: 7px 10px;
  display: flex;
  border-radius: 10px;
  align-items: center;
  &:active {
    opacity: 0.7;
  }
`;

export default Button;
